#include <stdio.h>

/**
* main - function to print name of function followed by newline
* @argc: unused argument
*
* @argv: argument whos first element [0] is the program name
*
* Return: (0)
*/

int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
